# 2026-06-01

## Bootstrap

- Created `.harness/agent.md` — the orchestrator (the Harness itself)
  for the converse-framework project.
- Created `.harness/reins/framework-dev/agent.md` — owns
  `converse_framework/` core (protocols, events, audio_utils, pipeline,
  utterance_collector, registry, transport, examples). Does NOT own
  `providers/`.
- Created `.harness/reins/provider-dev/agent.md` — owns
  `converse_framework/providers/` (mock, unavailable, silero,
  faster-whisper, llamacpp, kokoro-onnx, pocket-tts) and the lazy
  registrations in `registry.py`.
- Created `.harness/reins/tester/agent.md` — owns `tests/` and the
  regression contract (framework tests, harness regression, lightweight
  base import, provider lazy-loading).
- Created `.harness/docs/standards.md` — shared coding and test
  standards, referenced from each rein's `agent.md` body.

## Roster

- harness (orchestrator)
- framework-dev
- provider-dev
- tester

Total: 4 agents (1 orchestrator + 3 reins). Within the 3-7 coding-team
target from the init-harness skill. Skip code-reviewer for v0.1 — release
prep belongs to the orchestrator until publish is imminent.
